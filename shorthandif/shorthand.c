#include <stdio.h>
/**
 * if shorthand - also known as the ternary operator because it consists of three operands. It can be used to replace multiple lines of code with a single line. It is often used to replace simple if else statements
 * main - checks the code
 * Return: 0 always
 */
int main(void)
{
	//syntax:"variable = (condition) ? conditionTrue: conditionfalse;"//
	int (time < 18) ? prinf("Goood Day!") : printf("Good Morning");
	return (0);
}
